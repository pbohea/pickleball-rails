<% content_for :title, "For Artists" %>

<style>
  :root {
    --ink: #0f172a;
    --stone: #5b6475;
    --accent: #0d6efd;
    --accent-dark: #0b5ed7;
  }

  .page-shell { max-width: 1100px; margin: 0 auto; padding: 3rem 1.25rem 4rem; text-align: center; }

  .hero {
    background: radial-gradient(circle at 15% 20%, rgba(13, 110, 253, 0.12), transparent 35%),
                radial-gradient(circle at 80% 8%, rgba(15, 23, 42, 0.08), transparent 30%);
    padding: 3rem 2.4rem;
    border-radius: 18px;
    border: 1px solid #e5e7eb;
  }

  .eyebrow { display: inline-block; padding: 0.35rem 0.75rem; border-radius: 999px; background: var(--ink); color: #fff; font-weight: 600; letter-spacing: 0.02em; }
  .hero h1 { margin: 1rem 0 0.6rem; font-size: clamp(2.3rem, 4vw, 3.1rem); letter-spacing: -0.03em; color: var(--ink); line-height: 1.08; }
  .lede { font-size: 1.05rem; color: var(--stone); max-width: 780px; line-height: 1.6; margin: 0 auto 1.1rem; }

  .actions { display: flex; flex-wrap: wrap; gap: 0.75rem; align-items: center; justify-content: center; margin-top: 0.6rem; }
  .actions.actions--stacked { flex-direction: column; align-items: center; }
  .actions.actions--stacked .cta-primary,
  .actions.actions--stacked .pill-cta-btn { width: 100%; max-width: 320px; min-height: 54px; justify-content: center; }
  .cta-primary { display: inline-flex; align-items: center; gap: 0.4rem; background: var(--accent); color: #fff; padding: 0.9rem 1.5rem; border-radius: 10px; font-weight: 700; text-decoration: none; box-shadow: 0 10px 26px rgba(13, 110, 253, 0.22); transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s ease; }
  .cta-primary:hover { background: var(--accent-dark); transform: translateY(-1px); box-shadow: 0 12px 32px rgba(13, 110, 253, 0.26); }
  .cta-ghost { color: var(--ink); font-weight: 600; text-decoration: none; border-bottom: 2px solid #d9dce5; padding-bottom: 0.15rem; }
  .hero-media {
    margin: 1.75rem auto 0;
    max-width: 520px;
    width: 100%;
    aspect-ratio: 9 / 16;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 18px 48px rgba(15, 23, 42, 0.18);
    background: #000;
    position: relative;
  }
  .hero-media video {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    background: #000;
  }
  .hero-media img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  .pill-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1rem; margin: 2.3rem 0 3rem; }
  .pill { background: #fff; border: 1px solid #e5e7eb; border-radius: 14px; padding: 1.35rem; box-shadow: 0 14px 32px rgba(15, 23, 42, 0.05); text-align: center; }
  .pill-link { display: block; color: inherit; text-decoration: none; }
  .pill-link:hover { text-decoration: none; }
  .pill-cta-btn { display: inline-flex; align-items: center; justify-content: center; gap: 0.4rem; padding: 0.95rem 1.4rem; border-radius: 12px; border: none; background: linear-gradient(135deg, var(--accent-dark), var(--accent)); color: #fff; font-weight: 700; text-decoration: none; box-shadow: 0 10px 26px rgba(13, 110, 253, 0.28); transition: transform 0.15s ease, box-shadow 0.15s ease; animation: pulse-btn 1.8s ease-in-out infinite; }
  .pill-cta-btn:hover { text-decoration: none; transform: translateY(-1px) scale(1.01); box-shadow: 0 12px 32px rgba(13, 110, 253, 0.34); }
  .pill-cta-btn span { display: inline-flex; align-items: center; gap: 0.3rem; }
  @keyframes pulse-btn {
    0% { transform: scale(1); box-shadow: 0 10px 26px rgba(13, 110, 253, 0.22); }
    50% { transform: scale(1.02); box-shadow: 0 14px 32px rgba(13, 110, 253, 0.36); }
    100% { transform: scale(1); box-shadow: 0 10px 26px rgba(13, 110, 253, 0.22); }
  }
  .pill h3 { margin: 0 0 0.35rem; font-size: 1.08rem; color: var(--ink); }
  .pill p { margin: 0; color: var(--stone); line-height: 1.45; }

  .steps { margin: 0 0 3rem; }
  .steps h2 { margin: 0 0 1.1rem; font-size: 1.55rem; letter-spacing: -0.01em; color: var(--ink); }
  .step-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; }
  .step { background: #0f172a; color: #fff; border-radius: 14px; padding: 1.2rem; text-align: center; }
  .step strong { display: block; margin-bottom: 0.35rem; font-size: 1.03rem; }
  .step small { color: rgba(255, 255, 255, 0.88); line-height: 1.45; }

  .cta-end { background: #fff; border: 1px solid #e5e7eb; border-radius: 14px; padding: 1.7rem; text-align: center; box-shadow: 0 16px 32px rgba(15, 23, 42, 0.06); }
  .cta-end h2 { margin: 0 0 0.45rem; font-size: 1.85rem; color: var(--ink); }
  .cta-end p { margin: 0 0 1rem; color: var(--stone); }

  @media (max-width: 720px) {
    .page-shell { padding: 2.5rem 1.1rem 3rem; }
    .hero { padding: 2.4rem 1.6rem; }
    .pill-grid { grid-template-columns: 1fr; }
    .step-grid { grid-template-columns: 1fr; }
  }
</style>

<div class="page-shell">
  <header class="hero">
    <h1>Get more people to your shows</h1>
    <p class="lede">
      Post your show in 30 seconds.
    </p>
    <div class="actions <%= 'actions--stacked' if @featured_artist.present? %>">
      <% unless owner_signed_in? || artist_signed_in? || user_signed_in? || promoter_signed_in? %>
        <%= link_to "List your events", new_artist_lead_path(source: "artists_about"), class: "pill-cta-btn" %>
      <% end %>
      <% if @featured_artist.present? %>
        <%= link_to artist_path(@featured_artist), class: "pill-cta-btn" do %>
          <span>Check out a band's page</span>
        <% end %>
      <% end %>
    </div>
    <div class="hero-media">
      <video
        id="artist-hero-video"
        autoplay
        muted
        playsinline
        webkit-playsinline
        loop
        preload="auto"
        disablepictureinpicture
        controlslist="nodownload nofullscreen noremoteplayback"
      >
        <source src="https://pickleball-assets.s3.us-east-1.amazonaws.com/artist_example_formatted.mp4" type="video/mp4">
      </video>
    </div>

    <script>
      const artistVideo = document.getElementById('artist-hero-video');

      const playArtistInline = () => {
        if (!artistVideo) return;
        artistVideo.muted = true;
        artistVideo.playsInline = true;
        artistVideo.setAttribute('playsinline', '');
        artistVideo.setAttribute('webkit-playsinline', '');
        const attempt = artistVideo.play();
        if (attempt && typeof attempt.catch === 'function') {
          attempt.catch(() => {});
        }
      };

      document.addEventListener('turbo:load', playArtistInline);
      document.addEventListener('visibilitychange', () => {
        if (document.visibilityState === 'visible') playArtistInline();
      });

      artistVideo.addEventListener('webkitbeginfullscreen', () => {
        console.log('⚠️ Video went fullscreen (WKWebView config not applied)');
      });

      artistVideo.addEventListener('playing', () => {
        console.log('✅ Artist video is playing inline');
      });

      playArtistInline();
    </script>
  </header>

  <div class="pill-grid">
    <div class="pill">
      <h3>Live map</h3>
      <p>Connects fans to music at a bar - not the other way around.</p>
    </div>
    <div class="pill">
      <h3>Free, instant promotion</h3>
      <p>List every show for free. Bigger crowds mean better bar tabs and better bookings.</p>
    </div>
    <div class="pill">
      <h3>Get booked</h3>
      <p>Venues browse artists and invite you to play. Keep your calendar full.</p>
    </div>
    <div class="pill">
      <h3>Own your fans</h3>
      <p>Followers get notified when you add a date—no algorithms deciding who sees it.</p>
    </div>
  </div>

  <section class="steps">
    <h2>Three quick moves</h2>
    <div class="step-grid">
      <div class="step">
        <strong>1) Claim your profile</strong>
        <small>Add your name, socials, and genre in minutes.</small>
      </div>
      <div class="step">
        <strong>2) Drop your dates</strong>
        <small>Post shows for free—fans see them instantly on the map.</small>
      </div>
      <div class="step">
        <strong>3) Draw bigger crowds</strong>
        <small>Get booked by venues, earn more from packed rooms, and keep fans coming back.</small>
      </div>
    </div>
  </section>

  <%# <% unless owner_signed_in? || artist_signed_in? || user_signed_in? || promoter_signed_in? %>
  <%# <div class="cta-end">
  <%#   <h2>Put your next show where fans are looking.</h2>
  <%#   <p>Join free, list your dates, and get in front of crowds tonight.</p>
  <%#   <%= link_to "Sign up free", new_artist_registration_path, class: "cta-primary" %>
  <%# </div>
  <%# <% end %>
</div>
